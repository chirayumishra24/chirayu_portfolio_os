import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "ChirayuOS — Portfolio",
  description: "Chirayu Mishra's interactive desktop OS portfolio. Explore projects, skills, experience, and more in a macOS-inspired environment.",
  keywords: ["Chirayu Mishra", "portfolio", "developer", "full-stack", "Next.js", "React", "interactive"],
  authors: [{ name: "Chirayu Mishra" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
